# Statistical evidence charts

Statistical evidence is visualized beside the business domain it answers. The methods, denominators, effect sizes and intervals are registered in `reports/chart_registry.csv`; machine-readable numeric outputs live in `reports/statistics/`.

