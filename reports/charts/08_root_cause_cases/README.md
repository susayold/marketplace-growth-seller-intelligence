# Root-cause case charts

Case evidence is visualized in the domain folders and the decision layer to avoid duplicate image assets. Use `reports/root_cause_evidence.csv`, `reports/root_cause_hypothesis_matrix.csv`, and `reports/chart_registry.csv` to trace each case to its metric and chart.

